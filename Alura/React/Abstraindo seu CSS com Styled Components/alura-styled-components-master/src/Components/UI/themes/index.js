import { cores } from '../variables';

export const temaClaro = {
  body: cores.fundoClaro,
  inside: cores.conteudoClaro,
  text: cores.fundoClaroTxt,
  filter: '',
};
export const temaEscuro = {
  body: cores.fundoEscuro,
  inside: cores.conteudoEscuro,
  text: cores.fundoEscuroTxt,
  filter: 'invert(100)',
};
